[tool.poetry]
name = "sensor-gap-imputer"
version = "0.1.0"
description = "센서 데이터 시간 누락 감지 및 보간 후 INSERT SQL 생성 도구"
authors = ["leeyubeen"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
pandas = "^2.3.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
